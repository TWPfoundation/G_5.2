# G_5.2 Release-Candidate Notes — 2026-04-20

**Status:** operator checkpoint note for the clean Azure-only release-candidate baseline captured on commit `6d3408982b9384bfb35059baddb59ceb3b110c2e`.

## Scope

- In-scope provider for this checkpoint: `azure`
- Out-of-scope for this checkpoint: `anthropic`, `openai`, `gemini`
- Reason: as of April 20, 2026, the operator has live Azure credentials available in `.env`, while Anthropic and Gemini provider access is still pending and no separate OpenRouter-backed release-gate scope was available in the shell.

## Release-candidate facts

- Commit SHA: `6d3408982b9384bfb35059baddb59ceb3b110c2e`
- Canon version: `0.1.2`
- Prompt revision: `baseline-hardening-v1`
- Pipeline revision: `baseline-hardening-v1`
- Working tree at run time: clean (`dirty: false`)

## Prerequisite verification

The baseline-independent RC checks passed on this commit:

```bash
pnpm install
pnpm validate:canon
pnpm validate:witness
pnpm typecheck
pnpm test
pnpm smoke
```

## Captured report

- Azure report: `packages/evals/reports/eval-report-2026-04-20T13-02-38-698Z.json`
- Provider/model: `azure` / `gpt-5.4-2026-03-05`
- Command: `EVAL_PROVIDER=azure pnpm evals -- --trace`
- Result: `42/42` passed, `0` critical failures, exit code `0`

## Promotion status

This report **was** promoted to `packages/evals/gold-baselines/azure-0.1.2.json`.

## Drift review

This run is the new Azure gold baseline for canon `0.1.2` and prompt revision `baseline-hardening-v1`. It is critical-case clean and shows no subsystem drift relative to the promoted result, because the promoted result is this run itself.

## Earlier same-day failed attempt

An earlier Azure-only attempt on commit `1e2a226c8c5d0aa4df7d94198de6d09ae00a0a4c` captured:

- report: `packages/evals/reports/eval-report-2026-04-20T11-37-53-141Z.json`
- result: `38/42`, exit code `1`
- failed cases:
  - `artifact-overweight-001`
  - `continuity-fact-proposal-quality-001`
  - `memory-irrelevance-gating-001`
  - `reflection-brevity-restraint-001`

That failed checkpoint remains recorded in `docs/decision-log/0002-azure-rc-drift-2026-04-20.md` as the reason the first April 20 RC candidate was not promoted.

## Notes

- The clean run followed a prompt-discipline hardening pass on the same day, recorded on commit `6d34089`.
- The report was captured with `--trace` per the RC baseline procedure and promoted via `pnpm tsx scripts/refresh-gold-baseline.ts azure packages/evals/reports/eval-report-2026-04-20T13-02-38-698Z.json`.
- Grok and DeepSeek availability behind the Azure account do not alter the formal RC provider set for this repo; the supported release-gate providers remain `azure`, `anthropic`, `openai`, and `gemini`.
