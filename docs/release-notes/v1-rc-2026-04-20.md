# G_5.2 Release-Candidate Notes — 2026-04-20

**Status:** operator checkpoint note for the Azure-only release-candidate baseline attempt captured on commit `1e2a226c8c5d0aa4df7d94198de6d09ae00a0a4c`.

## Scope

- In-scope provider for this checkpoint: `azure`
- Out-of-scope for this checkpoint: `anthropic`, `openai`, `gemini`
- Reason: as of April 20, 2026, the operator has live Azure credentials available in `.env`, while Anthropic and Gemini provider access is still pending and no separate OpenRouter-backed release-gate scope was available in the shell.

## Release-candidate facts

- Commit SHA: `1e2a226c8c5d0aa4df7d94198de6d09ae00a0a4c`
- Canon version: `0.1.2`
- Prompt revision: `baseline-hardening-v1`
- Pipeline revision: `baseline-hardening-v1`
- Working tree at run time: clean (`dirty: false`)

## Prerequisite verification

The baseline-independent RC checks passed on this commit:

```bash
pnpm install
pnpm validate:canon
pnpm validate:witness
pnpm typecheck
pnpm test
pnpm smoke
```

## Captured report

- Azure report: `packages/evals/reports/eval-report-2026-04-20T11-37-53-141Z.json`
- Provider/model: `azure` / `gpt-5.4-2026-03-05`
- Command: `EVAL_PROVIDER=azure pnpm evals -- --trace`
- Result: `38/42` passed, `0` critical failures, exit code `1`

Failed cases:

- `artifact-overweight-001`
- `continuity-fact-proposal-quality-001`
- `memory-irrelevance-gating-001`
- `reflection-brevity-restraint-001`

## Promotion status

This report was **not** promoted to `packages/evals/gold-baselines/azure-0.1.2.json`.

The existing Azure gold baseline for canon `0.1.2` remains authoritative:

- `packages/evals/gold-baselines/azure-0.1.2.json`
- Baseline commit: `067564b44662d5dce4459b0fd6697824fcf84329`
- Baseline result: `42/42` passed

## Drift review

Compared with the current Azure gold baseline for the same canon and prompt revision, this run breaches the documented subsystem bands in `docs/drift-budget.md`:

- `editorial-workflow`: `100%` -> `50%` (`50` point gap; allowed `15`)
- `epistemics-and-meta`: `100%` -> `83.33%` (`16.67` point gap; allowed `10`)
- `memory-discipline`: `100%` -> `85.71%` (`14.29` point gap; allowed `10`)
- `reflection-discipline`: `100%` -> `75%` (`25` point gap; allowed `15`)

Because the drift is outside the allowed bands, this checkpoint is a release blocker for the Azure-only RC path until the failures are fixed or explicitly re-triaged through the decision log.

## Notes

- No critical cases failed, so this is not a schema-promotion refusal. It is a drift-budget failure against the current Azure gold baseline.
- The report was captured with `--trace` per the RC baseline procedure.
- Grok and DeepSeek availability behind the Azure account do not alter the formal RC provider set for this repo; the supported release-gate providers remain `azure`, `anthropic`, `openai`, and `gemini`.
